name: GridBot London Sunrise Sunset UI Chart Marker
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo London sunrise sunset UI chart marker
