name: Retired — GridAtlas mirror idempotence repair

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; the legacy GlobalGrid route is redirect-only.'
