name: Retired — GridAtlas mirror cartridge repair

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; no Atlas application or data cartridge is served from GlobalGrid2050.'
