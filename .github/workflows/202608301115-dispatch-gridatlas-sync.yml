name: Retired — GridAtlas mirror dispatcher

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; there is no GlobalGrid-hosted Atlas mirror to dispatch.'
