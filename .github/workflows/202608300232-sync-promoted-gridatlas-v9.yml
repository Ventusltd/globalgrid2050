name: Retired — GridAtlas mirror publisher

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired: GlobalGrid2050 no longer mirrors GridAtlas.'
          echo 'Canonical application: https://ventusltd.github.io/gridatlas/'
