import {animate,minute,row} from '../202609052154-minute-runtime/runtime.mjs';import {scenarioMWh} from './model.mjs';const root=document.getElementById('solar-75tw'),f=new Intl.NumberFormat('en-GB',{maximumFractionDigits:0});
if(root)animate(root,now=>{const cf=Number(root.querySelector('input').value)/100,ms=minute(now);if(cf<.05||cf>.4||!Number.isFinite(cf))return;row(root,'pv',f.format(scenarioMWh(ms/1000,cf))+' MWh',ms/60000);root.querySelector('[data-assumption]').textContent='Assumed capacity factor '+(cf*100).toFixed(0)+'%. At this hypothetical fleet size: '+f.format(scenarioMWh(60,cf))+' MWh per average minute. Not actual installed capacity or live generation.';});
