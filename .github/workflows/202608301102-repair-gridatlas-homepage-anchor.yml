name: Retired — local GridAtlas homepage repair

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; GlobalGrid2050 links to the canonical GridAtlas application.'
