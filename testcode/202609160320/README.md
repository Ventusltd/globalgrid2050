# CPU Wafer Galaxy - Usage bands

Stamp `202609160320`. Variation `bands-by-usage-fine` of the CPU Wafer Galaxy.

Usage quantised into eight shells, so the population at each level of reuse is visible.

## The law

```
r = R*(1-Math.floor(rad*8)/8) + shell(key)*B*1.5
theta = key x 2.39996...   (the golden angle)
```

A position is a pure function of the line's own permanent key and of published facts
about it. No randomness, no simulation state, no memory between frames - so the same key
lands on the same point on every device, for ever, and **a key issued tomorrow already
has a place**. That is what makes this surface unbounded rather than merely large.

**Identity is the key, never the coordinate.** Laws that place by measured usage move a
point when the usage sharpens; the line it names does not move. Chronology and density
are two readings of one permanently-numbered thing.

## Parameters

| | |
|---|---|
| point size | 0.7 |
| shell band | 14 |
| draw order | ascending |
| colour by | nature |

## Data

Draws `../202609142202/data/` - the published numbered pack. Nothing is computed here that the pack
does not already carry, and nothing is fetched that is computable from the key.

## Proof

`node proof/law.check.mjs` - and `--mutate` to show the checks can fail. It exercises the
real exported `place()` rather than a copy of it, and it states plainly that it cannot
check the drawing.

Generated by `_board/generate-variations.py`. A script does the labour; the board argues
the law.
