name: Retired — hourly GridAtlas catalogue sync

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired: the homepage uses the stable canonical GridAtlas pointer.'
          echo 'Canonical application: https://ventusltd.github.io/gridatlas/'
