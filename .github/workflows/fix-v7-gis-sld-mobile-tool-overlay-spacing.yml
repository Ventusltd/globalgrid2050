name: Fix V7 GIS SLD Mobile Tool Overlay Spacing

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix-mobile-tool-spacing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GRIDBOT_PAT }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Apply mobile tool overlay spacing fix
        run: python scripts/fix_v7_gis_sld_mobile_tool_overlay_spacing.py

      - name: Commit and push mobile tool overlay spacing fix
        env:
          PAT: ${{ secrets.GRIDBOT_PAT }}
        run: |
          git config --global user.name "gridbot"
          git config --global user.email "bot@globalgrid2050.com"

          git pull --rebase --autostash origin main
          git add \
            solar-bess-topology-v7/gis-sld-financial-sandbox/gis-sld-v5.css \
            scripts/fix_v7_gis_sld_mobile_tool_overlay_spacing.py \
            gridbot_reports/fix_v7_gis_sld_mobile_tool_overlay_spacing.md

          if git diff --cached --quiet; then
            echo "No mobile tool overlay spacing changes to commit."
            exit 0
          fi

          git commit -m "Fix V7 GIS SLD mobile tool overlay spacing"
          git pull --rebase --autostash origin main
          git push https://${PAT}@github.com/${{ github.repository }}.git HEAD:main
