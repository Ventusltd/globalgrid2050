name: Retired — Atlas mirror catalogue repair V2

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired; use the canonical GridAtlas application directly.'
